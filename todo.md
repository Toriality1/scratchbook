server: 🔧 Note: chalk is fine for dev, but in production, consider structured logging (e.g., pino, winston) — more on that below.
server: helmet for security headers
server: consider adding express json limit to 10kb or something around that (def is 100kb)
!return to qwen to finish service layer and more
